name: Build Check

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: dragonos/dragonos-dev:v1.1.0-beta3

    steps:
    - uses: actions/checkout@v3
      
    - name: build the DragonOS
      run: make
