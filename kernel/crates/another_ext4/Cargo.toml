[package]
name = "another_ext4"
version = "0.1.0"
edition = "2021"

[dependencies]
bitflags = "2.2.1"
log = "0.4"
spin = "0.9"

[features]
fuser_root_inode = []
# Exposes host recovery and fault-injection API surface solely to the
# integration harness.
# The DragonOS kernel dependency neither enables nor forwards this feature;
# ext4_test enables it explicitly in its independent host-test workspace.
test-api = []
